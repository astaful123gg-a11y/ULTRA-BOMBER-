services:
  - type: web
    name: brutal-bomber-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python brutal_bomber_api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
